#ifndef CONFIG_H
#define CONFIG_H

#define SYSTEM_TICK_MS 100      // System timer ticks every 100 milliseconds
#define MAX_TASKS 10            // Scheduler manages up to 10 tasks at once

#endif // CONFIG_H